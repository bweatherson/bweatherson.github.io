---
title: "Modal Logic: Tableaux Examples"
date: "2/18/2026"
format:
    revealjs:
        css: lecture-03-custom-tables.css
    beamer:
        include-in-header: 305-beamer-header-minimal.tex
---

# Example 10: $\Diamond \Box A \rightarrow A$ in KTB

## Example 10: $\Diamond \Box A \rightarrow A$ in KTB

All the rules of **KT** still hold in **KTB**.

The only difference is that there are now some new rules telling us that we can apply True $\Box$ sentences and False $\Diamond$ sentences back up the tree.

But since all the old rules hold, it's fine to just start with where we ended last time.

---

\begin{oltableau}
[\sFmla{\False}{1, \Diamond \Box A \rightarrow A}, checked, just = \TAss
    [\sFmla{\True}{1, \Diamond \Box A}, checked, just = {\TRule{\False}{\rightarrow}[1]}
        [\sFmla{\False}{1, A}, just = {\TRule{\False}{\rightarrow}[1]}
            [\sFmla{\True}{1.1, \Box A}, checked, just = {\TRule{\True}{\Diamond}[2]}
                [\sFmla{\True}{1.1, A}, just = {\TRule{\Box}{\text{T}}[4]}
                ]
            ]
        ]
    ]
]
\end{oltableau}

::: {.content-visible when-format="revealjs"}
![$\Diamond \Box A \rightarrow A$](../images/tableaux-11/tableau-11-40.tex.svg){width=600px}
:::

Here's where we were.

---

\begin{oltableau}
[\sFmla{\False}{1, \Diamond \Box A \rightarrow A}, checked, just = \TAss
    [\sFmla{\True}{1, \Diamond \Box A}, checked, just = {\TRule{\False}{\rightarrow}[1]}
        [\sFmla{\False}{1, A}, just = {\TRule{\False}{\rightarrow}[1]}
            [\sFmla{\True}{1.1, \Box A}, checked, just = {\TRule{\True}{\Diamond}[2]}
                [\sFmla{\True}{1.1, A}, just = {\TRule{\Box}{\text{T}}[4]}
                    [\sFmla{\True}{1, A}, just = {\TRule{\Box}{\text{B}}[4]}, close]
                ]
            ]
        ]
    ]
]
\end{oltableau}

::: {.content-visible when-format="revealjs"}
![$\Diamond \Box A \rightarrow A$](../images/tableaux-11/tableau-11-41.tex.svg){width=600px}
:::

By applying the distinctive rule for **B** we get $A$ being true at the one place we've said it can't be true. So the tree closes, and in **KTB** this is valid.


